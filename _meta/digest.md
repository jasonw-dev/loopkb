# Run digest

No runs yet.

<!-- Framework-managed: every `kb-loop` run overwrites this file with its report
     (see `_meta/loop.md` → The digest). Past digests are in git history — do not
     archive them here. `scripts/lint.py` never schema-checks `_meta/`. -->
